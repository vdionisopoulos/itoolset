      <!-- Footer -->
      <footer class="footer">
        <div class="container-fluid">
          <div class="row text-muted">
            <div class="col-6 text-start">
              <p class="mb-0">© 2025 <strong>SLAlytics</strong></p>
            </div>
            <div class="col-6 text-end">
              <a class="text-muted" href="/changelog">Changelog</a>
            </div>
          </div>
        </div>
      </footer>
    </div> <!-- end .main -->
  </div> <!-- end .wrapper -->

  <!-- Feather Icons -->
  <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>

  <!-- Bootstrap Bundle (if needed for collapse/toggle behavior) -->
  <script src="/js/bootstrap.bundle.min.js"></script>

  <!-- Main JS (AdminKit and your custom behavior) -->
  <script src="/js/app.js"></script>

  <!-- Optional: Page-specific script -->
  <% if (typeof script !== 'undefined') { %>
    <script src="/js/<%= script %>"></script>
  <% } %>

  <!-- Sidebar toggle + icon init -->
  <script>
    document.addEventListener("DOMContentLoaded", function () {
      if (typeof feather !== 'undefined') {
        feather.replace();
      }

      const toggle = document.querySelector(".js-sidebar-toggle");
      const wrapper = document.querySelector(".wrapper");

      if (toggle && wrapper) {
        toggle.addEventListener("click", function () {
          wrapper.classList.toggle("toggled");
        });
      }
    });
  </script>
</body>
</html>
